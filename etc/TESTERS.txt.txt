After unzipping the project:

1. Open up a terminal(command line)
2. Navigate to the folder where you extracted the project
3. Run "mvn compile exec:java"
4. Open up a web browser and go to "localhost:4567"
5. Open up a new incognito browser (Ctrl Shift N) and navigate to "localhost:4567"
6. When ready, begin testing.